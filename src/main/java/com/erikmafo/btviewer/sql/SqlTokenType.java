package com.erikmafo.btviewer.sql;

public enum SqlTokenType {
    SELECT,
    IDENTIFIER,
    ASTERISK,
    COMMA,
    QUOTED_STRING,
    INTEGER,
    BOOL,
    FROM,
    WHERE,
    OPERATOR,
    AND,
    LIMIT,
}
