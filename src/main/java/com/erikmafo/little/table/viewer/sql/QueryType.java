package com.erikmafo.little.table.viewer.sql;

/**
 * An enumeration of the supported types of queries. The query type must always be the first
 * statement of the query expression.
 */
public enum QueryType {
    SELECT
}
